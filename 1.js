/**
 * =======================
 * Will you pass the test?
 * =======================
 * 
 * Description
 * -----------
 * Seorang murid di Sekolah Hacktiv8 ingin mengetahui nilai akhir dari
 * ujian livecode yang dia lakukan 3 kali. Nilai akhir didapat dari rata-rata ketiga nilai
 * livecode dan ditampilkan dalam skala huruf A-E dengan detail sebagai berikut:
 * A -> 80 - 100
 * B -> 70 - 79
 * C -> 60 - 69
 * D -> 50 - 59
 * E -> 0 - 50
 * 
 * Instruction
 * -----------
 * Diketahui nilai murid tersebut adalah 70, 80, dan 90
 * Buatlah pseudocode untuk mencari nilai akhir murid tersebut.
 */

// Write pseudocode here

// SET nilai1 WITH VALUE 70
// SET nilai2 WITH VALUE 80 
// SET nilai3 WITH VALUE 90
// SET average WITH VALUE (nilai1 + nilai2 + nilai3) / 3

//IF average >= 80 AND average <= 100 THEN
    // DISPLAY 'A'
//ELSE IF average >= 70 AND average <= 79 THEN
    // DISPLAY 'B'
//ELSE IF average >= 60 AND average <= 69 THEN
    //DISPLAY 'C'
//ELSE IF average >= 50 AND average <= 59 THEN
    //DISPLAY 'D'
//ELSE IF average >= 0 AND average <= 49 THEN
    // DISPLAY 'E'
// ENDIF
